---
# Domain Validation Role Defaults
# Default values for validation settings
# Can be overridden in inventory group_vars or host_vars

# No default values currently required
# This role uses validation logic without configurable timeout/retry parameters
# If needed in the future, add them here and they can be overridden in inventory

