---
galaxy_info:
  role_name: vmware_provisioning
  author: DevOps Team
  description: VMware VM provisioning role
  min_ansible_version: "2.14"
  platforms:
    - name: VMware
      versions:
        - all

dependencies:
  - role: provision/vcenter_connection
